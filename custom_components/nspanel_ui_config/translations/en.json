{
  "config": {
    "step": {
      "user": {
        "title": "NSPanel UI Config",
        "description": "Configuration layer for the AppDaemon backend (nspanel-lovelace-ui).",
        "data": {
          "output_path": "Output path of the generated YAML (inside the shared bind mount)",
          "reload_mode": "AppDaemon reload after generating",
          "import_yaml_path": "Optional: path of an existing apps.yaml to import"
        }
      }
    },
    "abort": {
      "already_configured": "NSPanel UI Config is already set up."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "NSPanel UI Config – Options",
        "data": {
          "output_path": "Output path of the generated YAML",
          "reload_mode": "AppDaemon reload after generating",
          "import_yaml_path": "Optional: path of an existing apps.yaml to import"
        }
      }
    }
  }
}
